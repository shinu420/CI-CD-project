Set-Content -Path package.json -Value '{
  "name": "flask-app",
  "version": "1.0.0",
  "description": "My flask app",
  "main": "app.js",
  "scripts": {
    "test": "echo \"No test defined\" && exit 0"
  },
  "dependencies": {}
}' -Encoding ascii
